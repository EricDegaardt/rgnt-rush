
import React from 'react';

const CollectionEffect = () => {
    return (
        <div 
            className="absolute inset-0 bg-green-400 bg-opacity-40 animate-fade-out pointer-events-none"
            style={{ animationFillMode: 'forwards' }}
        />
    );
};

export default CollectionEffect;
